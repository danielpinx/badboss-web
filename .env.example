# Redis 연결 URL (M-8: 인증 패스워드 포함)
REDIS_URL=redis://:badboss-dev@localhost:6379

# Redis 인증 패스워드 (Docker Compose에서 사용)
REDIS_PASSWORD=badboss-dev

# CORS 허용 Origin (H-1: 프로덕션에서 특정 도메인으로 변경)
# ALLOWED_ORIGIN=https://your-domain.com
